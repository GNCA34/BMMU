\input{./preamble.tex}
% \input{./glossaries.tex}
\input{./macros.tex}

\begin{document}

\title{\bf \huge  KOCAELİ ÜNİVERSİTESİ \newline Bilgisayar Mühendisliğinde Matematik Uygulamaları  }

\author{
Goncagül Koçak 200201108}
\date{Update on \today}
\maketitle
\setcounter{tocdepth}{2}
\setcounter{minitocdepth}{1} 

\begin{multicols}{2}
    \dominitoc% Initialization
    \adjustmtc[3]% chp number shift
   
\end{multicols}
\addcontentsline{toc}{section}{Problem Tanımı}
\section*{Problem Tanımı:}
\section{} Logistic Regression const function \newline \newline\newline
J(\theta)=(-1/m)
$\sum_{i=1}^{m}$
\left [ (y^i)log(h_\theta(x^i))
+(1-y^i)log(1-h_\theta(x^i))\right]
\newline
\newline
\newline
\partial(J\theta)/\partial(\theta_j)
=(-1/m)
$\sum_{i=1}^{m}$
\left [ ((y^i)(\partial/\partial(\theta_j))
(h_\theta(x^i))/(h_\theta(x^i))+
((1-y^i)(\partial/\partial(\theta_j))
(1-h_\theta(x^i))/(1-h_\theta(x^i))]\right
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left [ ((y^i)(\partial/\partial(\theta_j))
\sigma(\theta^T(x^i)))/(h_\theta(x^i))+
((1-y^i)(\partial/\partial(\theta_j))
(1-\sigma(\theta^T(x^i))))/
(1-(h_\theta(x^i)))\right]
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left[(y^i(\sigma(\theta^T(x^i)))
(1-(\sigma(\theta^T(x^i)))
(\partial/\partial(\theta_j))
\sigma(\theta^T(x^i)))/(h_\theta(x^i)))\right]
\newline
\newline
\left[-(1-y^i)(\sigma(\theta^T(x^i)))(1-(\sigma(\theta^T(x^i)))(\partial/\partial(\theta_j))
\sigma(\theta^T(x^i)))/(1-h_\theta(x^i)))(\theta^T(x^i)))/(1-h_\theta(x^i))\right]
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left[(y^ih_\theta(x^i)(1-h_\theta(x^i))))\partial/\partial(\theta_j))(\theta^T(x^i)))/(h_\theta(x^i))\right]
\newline
\newline
-((1-y^i)h_\theta(x^i)(1-h_\theta(x^i))(\partial/\partial(\theta_j))(\theta^T(x^i)))/(1-h_\theta(x^i)))
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left[(y^i(1-h_\theta(x^i))x_j^i)-((1-y^i)
h_\theta(x^i)x_j^i)\right]
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left[x_j^i(y^i(1-h_\theta(x^i))-(1-y^i)h_\theta(x^i))\right]
\newline
\newline
\newline
=(-1/m)
$\sum_{i=1}^{m}$
\left[x_j^i((y^i)-(y^i)h_\theta(x^i)-h_\theta(x^i)+y^ih_\theta(x^i))\right]
\newline
\newline
\newline
=(1/m)
$\sum_{i=1}^{m}$(h_\theta(x^i)-y^i)x_j^i

\section{} Çıkarımda kullandığım eşitlikler
\newline
\newline
 1-)
 h_\theta(x)=\sigma(\theta^T(x^i))
\newline
\newline
2-) \partial/\partial(x)\sigma((x))
=\partial/\partial(x)(1/(1+e^(-x))
=\sigma((x))(1-\sigma((x)))
\newline
\newline
3-)
 \partial/\partial(\theta^T)(\theta^T(x^i))
=x_j^i
\end{document}

